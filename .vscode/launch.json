{
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Launch index.html",
            "url": "https://localhost:8080/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}